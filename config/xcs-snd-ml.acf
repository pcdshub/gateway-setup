HAG(mlhosts) {snd-model-0}

ASG(DEFAULT) {
    RULE(1,READ){
      HAG(mlhosts)
    }
}

ASG(RWML) {
    RULE(1,READ){
      HAG(mlhosts)
    }
    RULE(1,WRITE,TRAPWRITE){
      HAG(mlhosts)
    }
}

ASG(NOACCESS) {
    RULE(1,NONE)
}

