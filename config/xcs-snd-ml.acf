HAG(mlread) {snd-sase-model-0}
HAG(mlwrite) {snd-model-0}

ASG(DEFAULT) {
    RULE(1,READ){
      HAG(mlread,mlwrite)
    }
}

ASG(RWML) {
    RULE(1,READ){
      HAG(mlread,mlwrite)
    }
    RULE(1,WRITE,TRAPWRITE){
      HAG(mlwrite)
    }
}

ASG(NOACCESS) {
    RULE(1,NONE)
}

