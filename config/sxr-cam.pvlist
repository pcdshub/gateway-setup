EVALUATION ORDER DENY, ALLOW

.* DENY

# Allow camera images read-only
SXR:.*IMAGE.*		ALLOW
SXR:.*Image.*		ALLOW
SXR:.*PROJ.*		ALLOW
SXR:.*HPrj.*		ALLOW

# SXR Spectrometer IMAGE_CMPX and LIVE_IMAGE_FAST specifically blocked
# Problem: Looks like the gateway continues to monitor these large images
# even though the client is told NOACCESS.
# Just the lookup seems to make the gateway monitor it.
#SXR:EXS:CVV:01:IMAGE_CMPX.*			ALLOW NOACCESS 1
#SXR:EXS:CVV:01:LIVE_IMAGE_FAST.*	ALLOW NOACCESS 1
