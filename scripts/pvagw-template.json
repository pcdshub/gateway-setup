/* Autogenerated configuration for $(xxx) gateway on pscag$(N) */
{
    "version":1,
    "readOnly":false,
	"statsdb":"statsdb.sql",
    "clients":[
        {
            "name":"theClient",
            "provider":"pva",
            "addrlist":"$(SUBNET)_IF$(N) $(SUBNET)_BC",
            "autoaddrlist":false
        }
    ],
    "servers":
	[
        /* Create a server with an interface for each subnet we want to serve */
        {
            "name":"theServer",
            "clients":["theClient"],
            /* "interface":[	','.join(PSCAG$(N)_IFLIST - $(SUBNET)_IF$(N)) ], */
            "interface":[],
            "autoaddrlist":true,
            "access":"/reg/g/pcds/gateway/config/pcds-access.acf",
            "pvlist":"/reg/g/pcds/gateway/config/$(xxx).pvlist",
            "statusprefix":"PVAG:$(XXX):"
        },
        /* allow client side to access GW status PVs */
        {
            "name":"statusServer",
            "clients":[], /* status PVs only */
            "interface":["$(SUBNET)_IF$(N)"],
            "addrlist":"$(SUBNET)_BC",
            "autoaddrlist":false,
            "statusprefix":"PVAG:$(XXX):"
        }
    ]
}
